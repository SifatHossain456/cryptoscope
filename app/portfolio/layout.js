export const metadata = {
  title: 'Portfolio Tracker',
  description: 'Track any Ethereum wallet — see token holdings, live prices, and portfolio allocation breakdown.',
}

export default function PortfolioLayout({ children }) {
  return children
}
